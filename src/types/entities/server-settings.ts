export default class ServerSettings {
  embedColor: string = 'the color embed will have';

  prefix: string = 'the prefix will be used in the server';

  guestRoleId: string =
    'optional role can be add to a new user when join the server';

  welcomeChannelId: string = 'the channel where greetings will be';
}
